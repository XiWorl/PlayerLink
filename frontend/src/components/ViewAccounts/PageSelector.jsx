import "./PageSelector.css"

export default function PageSelector({ pages, selectedPage, setSelectedPage }) {
	//TODO: This component will allow the user to select a page to load data from the backend
}
